[package]
name = "koett-engine"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
cpal = "=0.18.1"
ringbuf = "=0.5.1"
sherpa-onnx = { version = "=1.13.5", default-features = false, features = ["static"] }
